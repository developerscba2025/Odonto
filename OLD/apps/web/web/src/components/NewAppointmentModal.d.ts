interface Props {
    isOpen: boolean;
    onClose: () => void;
    initialDate?: string;
}
export default function NewAppointmentModal({ isOpen, onClose, initialDate }: Props): import("react/jsx-runtime").JSX.Element;
export {};
